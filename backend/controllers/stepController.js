import Step from '../models/stepModel.js';

export async function advanceToNextStep() {
    //Avance a la step suivante si tous les matchs de la step actuelle sont finis et que tous les scores sont rentrés
    //Effectue les verifications nécessaires pour déterminer les joueurs gagnants et met a jour les associations entre Step et Matchs

}